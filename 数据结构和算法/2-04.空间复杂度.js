/**
 * 空间复杂度
 */

function fn(arr = []) {
    // O(1)
    const a = arr[0]
    //...
    const b = arr[n]
}

function fn(arr = []) {
    // O(n)

    const arr2 = [];
    for (let i = 0; i < arr.length; i++) {
        arr2[i] = arr[i]
    }
}